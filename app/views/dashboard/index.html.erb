<% content_for :top_content do %>
  <!--
  <div class="jumbotron">
    <div class="container">
      <header>
        <% if user_signed_in? %>
          <h1>Welcome to Fidiyo, <%= current_user.email %>!</h1>
          <h2>We're using Rails <%= Rails.version %></h2>
          <h1>Rails:<%= Rails.env %></h1>
        <% else %>
          <h1>Welcome to Fidiyo!</h1>
          <p>Register to get started.</p>
          <p>
            <%= link_to 'Register', new_user_registration_path, class: 'btn btn-primary btn-lg' %>
          </p>
        <% end %>
      </header>
    </div>
  </div>
<% end %>-->


<!--use for single page js files that are precompiled. will load them into head. if use yield :head -->

<% content_for :head do %>
  <%= javascript_include_tag 'main', 'id': 'jsmain'%>
<% end %>
<% if user_signed_in? %>
  <section>
    <view profilepic = "http://localhost/dashboard/upload/17b01814f11323b70ab18c7fd9539926.webp" authenticity_token="<%= form_authenticity_token %>"></view>
  </section>
<%= javascript_pack_tag "hub" %>
<% end %>
